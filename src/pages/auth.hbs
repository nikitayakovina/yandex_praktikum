<div class="auth">
    <div class="auth__container">
        <header class="auth__title">
            <span class="auth__title__text">{{ title }}</span>
        </header>
        <main class="auth__form">
            {{#each inputs}}
                {{> Input this}}
            {{/each}}
        </main>
        <footer>
            {{> Footer footer }}
        </footer>
    </div>
</div>