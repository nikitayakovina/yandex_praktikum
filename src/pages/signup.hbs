<div class="signup">
    <div class="signup__container">
        <header class="signup__title">
            <h1 class="signup__title__text">{{ title }}</h1>
        </header>
        <main class="signup__form">
            {{#each inputs}}
                {{> Input this}}
            {{/each}}
        </main>
        <footer>
            {{> Footer footer }}
        </footer>
    </div>
</div>